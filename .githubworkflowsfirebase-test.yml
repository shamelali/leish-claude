name: Firebase CI Testing
on:
  push:
<<<<<<< HEAD
    branches:
      - main
jobs:
  test_hashing:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Run senangPay Test
        run: |
          cd functions
          node test.js
=======
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test_and_lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Dependencies
        run: |
          npm install
          cd functions && npm install

      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Cache Firebase Emulators
        uses: actions/cache@v4
        with:
          path: ~/.cache/firebase/emulators
          key: ${{ runner.os }}-firebase-emulators-${{ hashFiles('~/.cache/firebase/emulators/**') }}

      - name: Run Firebase Emulator Tests
        run: firebase emulators:exec "npm test"
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}

      - name: Deploy to Firebase (Preview)
        run: firebase deploy --only functions,hosting --token "${{ secrets.FIREBASE_TOKEN }}"
>>>>>>> 4737ffe (chore: verified senangPay hash logic and added automated tests)
